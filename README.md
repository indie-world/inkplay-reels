# inkplay-reels

Public CDN host for Inkplay reel videos, served via jsDelivr. Instagram publishing requires a public video URL, and the main Inkplay code repo is private, so the auto-posted marketing reel clips live here. Auto-managed by the reel pipeline.
